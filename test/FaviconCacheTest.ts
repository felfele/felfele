import { FaviconCache } from '../src/FaviconCache';

beforeEach(() => {
});

afterEach(() => {
});

test('Basic favicon test', async () => {
    const url = 'https://www.github.com';
    const expected = 'https://github.com/apple-touch-icon.png';

    // const result = await FaviconCache.getFavicon(url);

    // expect(true).toBe(true);
})
