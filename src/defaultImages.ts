import { ImageDataRequiredSource } from './models/ImageData';

export const defaultImages = {
    balloon: require('../images/balloon.svg') as ImageDataRequiredSource,
    bug: require('../images/bug.svg') as ImageDataRequiredSource,
    compass: require('../images/compass.svg') as ImageDataRequiredSource,
    ghost: require('../images/ghost.svg') as ImageDataRequiredSource,
    iconWhiteTransparent: require('../images/icon-white-transparent.png') as ImageDataRequiredSource,
    link: require('../images/link.svg') as ImageDataRequiredSource,
    snorkeling: require('../images/snorkeling.svg') as ImageDataRequiredSource,
    defaultUser: require('../images/assets/defaultuser.png') as ImageDataRequiredSource,
    felfeleAssistant: require('../images/felfele-assistant.png') as ImageDataRequiredSource,
};
